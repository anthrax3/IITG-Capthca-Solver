Input : An array of numbers.

Let the sum of black pixels between two ends of a
segment denote the waste information. Then, we need
to minimize the waste information. A cut is only allowed along a line where
the first or last column with at least one white pixel occurs.
